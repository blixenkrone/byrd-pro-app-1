import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class CoreService {
    constructor() { }

    // TODO: If a core func depends on something (possibly auth/user service?)
}
